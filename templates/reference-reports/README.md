# Reference weekly reports

Put prior human-written weekly report DOCX files here when you want the AI weekly report generator to use them as style references.

Supported files:
- `.docx`
- `.txt`

The workflow does not train the model permanently. It reads up to 5 files in this folder at each run and includes their extracted text as temporary style examples for that report generation only.
